import { Meta } from "@storybook/addon-docs";

<Meta title="Slots Spike" />

<style>{`
`}</style>

# Slots Spike

This is a series of stories around controls that use props.children and slot attributes.

# Stories

- I can specify a primitive value as the content of a container control.
- I can specify a props as the content of a container control. (attributes only)
